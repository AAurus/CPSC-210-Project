package utility;

import enums.*;
import model.*;

import java.util.Arrays;
import java.util.List;

public class ListOfHelper {
    ///A utility class SOLELY CREATED to replicate the ListOfHelper.listOf(E... elements) method.

    //EFFECTS: returns a List containing the elements passed in
    public static List<ScoreType> listOf(ScoreType... elements) {
        return Arrays.asList(elements);
    }

    //EFFECTS: returns a List containing the elements passed in
    public static List<StatType> listOf(StatType... elements) {
        return Arrays.asList(elements);
    }

    //EFFECTS: returns a List containing the elements passed in
    public static List<ProficiencyType> listOf(ProficiencyType... elements) {
        return Arrays.asList(elements);
    }

    //EFFECTS: returns a List containing the elements passed in
    public static List<ModifierType> listOf(ModifierType... elements) {
        return Arrays.asList(elements);
    }

    //EFFECTS: returns a List containing the elements passed in
    public static List<Feature> listOf(Feature... elements) {
        return Arrays.asList(elements);
    }

    //EFFECTS: returns a List containing the elements passed in
    public static List<Proficiency> listOf(Proficiency... elements) {
        return Arrays.asList(elements);
    }

    //EFFECTS: returns a List containing the elements passed in
    public static List<InventoryItem> listOf(InventoryItem... elements) {
        return Arrays.asList(elements);
    }

    //EFFECTS: returns a List containing the elements passed in
    public static List<String> listOf(String... elements) {
        return Arrays.asList(elements);
    }

    //EFFECTS: returns a List containing the elements passed in
    public static List<CharClass> listOf(CharClass... elements) {
        return Arrays.asList(elements);
    }

    //EFFECTS: returns a List containing the elements passed in
    public static List<CharRace> listOf(CharRace... elements) {
        return Arrays.asList(elements);
    }
}
